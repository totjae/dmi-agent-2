# DMI Agent Workflow

AGENT_ID: AGENT_2

## 실행 입력

1. 이 파일을 읽는다.
2. `/prompt/AGENT_PROMPT.md`를 읽고 그 지시로 독립 분석한다.
3. 후보·순위·판단을 모두 확정한 뒤 `/templates/OUTPUT.md`를 읽어 저장 형식을 적용한다.

`/prompt/AGENT_PROMPT.md`가 없거나 비어 있으면 실행하지 말고 `PROMPT_UNAVAILABLE`을 보고한다.

## 사용 문서 버전 기록

각 실행 지침을 실제로 읽을 때 repository, path, 읽은 content의 blob SHA, 조회에 사용한 commit SHA(확인 가능할 때), READ_AT_KST를 보관한다. /WORKFLOW.md, /prompt/AGENT_PROMPT.md, /templates/OUTPUT.md 세 파일을 OUTPUT의 DOCUMENT_VERSIONS에 기록한다. 템플릿을 읽는 시점은 기존 실행 순서를 따른다.

SHA는 해당 내용을 반환한 조회 응답 또는 검증된 revision에서만 가져온다. 실행 종료 시점의 최신 main SHA를 과거에 읽은 버전처럼 기록하지 않는다. commit SHA와 blob SHA를 혼용하지 않는다. blob SHA만 확인되면 commit은 N/A로 둔다. 버전 확인 실패만으로 분석을 중단하지 않고 해당 값은 N/A, 상태와 사유는 UNVERIFIED로 기록한다. 이후 리뷰는 버전 미확인 결과를 특정 버전의 성과로 묶지 않는다.

## 격리 규칙

이 실행에서 읽을 수 있는 GitHub 저장소는 현재 저장소 하나뿐이다.

읽지 않는 대상:
- 현재 저장소의 기존 `/runs/**` 파일 내용. 아래 저장 경로 확인 및 이번 실행 검증 예외만 허용한다.
- 다른 DMI 에이전트 저장소
- `totjae/dmi-market-pipeline`
- 과거 리뷰와 다른 Prediction 결과
- 다른 에이전트의 프롬프트

실행 지침은 위에 지정된 정확한 파일 경로로 조회한다. 기존 결과의 본문·발췌문이 노출되는 저장소 전체 내용 검색은 하지 않는다. 외부 시장 자료를 찾기 위한 웹 검색은 허용한다.

다음은 저장 절차에 한정한 열람 예외다.
- 후보·순위·판단 확정 후, 대상 날짜 디렉터리의 파일명·경로·SHA 등 내용 없는 메타데이터만 조회하여 저장 경로의 존재 여부와 재실행 번호를 확인할 수 있다. 기존 결과 본문은 열지 않는다.
- 이번 도구 호출에서 생성 성공이 확인된 정확한 파일 경로와 commit/blob SHA를 기록한다. 이 파일만 저장 후 재열람하여 확정된 보고서와 대조한다. 가능하면 반환된 commit을 지정해 읽는다.
- 저장 응답이 불명확하면 이번에 제출한 정확한 경로 하나만 확인할 수 있다. 제출 내용과 완전히 일치하고 이번 실행의 파일임을 확인할 수 있을 때만 저장된 것으로 처리한다. 다르거나 소유 실행을 확인할 수 없으면 덮어쓰거나 분석에 사용하지 않고 충돌을 보고한다.
- 검증을 위한 열람은 후보 재선정·순위 변경·새로운 시장 분석의 입력으로 사용하지 않는다.

금지된 결과가 우연히 노출돼도 종목명 자체를 금지하지 않는다. 다른 실행의 후보·순위·근거·결론을 사용하지 말고, 외부 원출처에서 독립적으로 확인한 사실만 사용한다. 노출이 판단에 영향을 줬거나 분리할 수 없으면 저장하지 않고 실패 이유를 보고한다.

## 시간과 정보

- 시간대는 Asia/Seoul이다. DATE는 예약이 대상으로 삼는 거래일 YYYY-MM-DD, RUN_TIME_KST는 정규 슬롯 03:30 또는 08:30이다. 실제 지연 시작 시각으로 슬롯이나 경로를 바꾸지 않는다.
- 두 슬롯은 각각 독립된 NORMAL 실행이다. 08:30은 그때까지의 새 정보로 처음부터 분석하며 03:30 결과를 읽거나 이어받지 않는다.
- DATA_CUTOFF_KST와 SCHEDULED_AT_KST는 DATE의 해당 슬롯으로 고정한다. 실제 시작이 늦어져도 기준시각을 뒤로 미루지 않는다.
- STARTED_AT_KST는 실제 시작, ANALYSIS_TIME_KST는 후보·순위·판단 확정, REPORT_COMPLETED_AT_KST는 제출 문서 완성 시각이다. 모두 +09:00이 포함된 ISO 8601로 기록한다. 저장 완료 시각은 문서 작성 시 추측하지 않고 실제 저장 commit 시각으로 별도 검증한다.
- 가격 관측시각과 정보 최초 공개시각이 cutoff 이하여야 한다. 나중에 수정된 기사·최신 누적 체결·종가를 과거 슬롯 자료인 것처럼 사용하지 않는다. 당시 이용 가능 여부를 검증하지 못한 값은 N/A로 둔다.
- 확인되지 않은 가격·체결·수급·공시는 만들지 않는다. 지침과 에이전트 프롬프트 사이에 명시적인 충돌이 있으면 프롬프트를 수정하거나 임의로 우회하지 말고 보고한다.

## 실행 유형

- 정규 실행: 각 DATE·슬롯에서 NORMAL / RERUN_SEQUENCE 0. 08:30은 03:30의 rerun이 아니다.
- 오류 복구: 동일 DATE·슬롯·cutoff를 유지하는 RECOVERY. RERUN_SEQUENCE는 파일명의 번호와 일치한다. 기존 결과를 읽어 복사하지 않고 허용된 당시 정보만 사용한다.
- 더 늦은 새 정보를 사용하는 분석을 과거 슬롯의 RECOVERY로 저장하지 않는다. 정규 08:30 외 추가 분석은 별도 요청과 식별 규칙을 확정한 뒤 진행한다.
- 정규장 개장 이후 저장된 결과도 지연 사실과 실제 시각을 숨기지 않는다. 장전 공식 비교 채택 여부는 리뷰가 실제 최초 저장시각과 해당일 KRX 개장시각으로 판정한다.

## 저장

정상 경로:

`runs/YYYY-MM-DD/HHMM.md`

HHMM은 실제 시작시각이 아니라 RUN_TIME_KST의 0330 또는 0830이다.

같은 경로의 파일이 이미 존재하면 내용을 읽거나 덮어쓰지 않는다. 동일 cutoff의 오류 복구로 명시된 실행에 한해서 다음 경로를 사용한다. 그렇지 않은 중복 예약 실행은 DUPLICATE_SLOT로 종료한다:

`runs/YYYY-MM-DD/HHMM_rerun_01.md`

해당 날짜·시각의 파일명 목록에서 가장 높은 재실행 번호 다음의 비어 있는 경로를 사용한다. 번호는 최소 두 자리로 표기한다. 저장은 새 파일 생성으로 수행하며, 동시 실행 등으로 경로가 이미 생겼다면 덮어쓰지 않고 충돌을 보고한다.

저장 후 위 열람 예외에 따라 이번 실행 파일만 다시 읽어 다음을 확인한다.
- `DMI_RUN_META`, `STAGE_REPORT`, `STAGE_RESULT` wrapper
- AGENT_ID, 날짜, 시각
- 후보 수와 결과 행 수의 일치
- 종목코드 6자리 또는 `N/A`
- 본문과 결과 capsule의 후보·순위 일치

실제 저장과 재검증이 완료된 경우에만 `SUCCESS`로 보고한다.
